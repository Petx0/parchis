[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "parchis"
version = "0.1.0"
description = "Parchís RL environment and training scripts"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
